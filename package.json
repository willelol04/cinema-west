{
  "name": "cinema",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
    "cacheDirectories": [
    "frontend/node_modules"
  ],

"scripts": {
    "heroku-postbuild": " npm i && cd frontend && npm install && npm run build",
  },
  "dependencies": {
    "vite": "^7.3.0"
  }
}
